[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "creator-course-pipeline"
version = "0.2.1"
description = "Safe Agent Skills for verified course-video packages and unpublished drafts"
requires-python = ">=3.11"
license = {file = "LICENSE"}

[tool.pytest.ini_options]
testpaths = ["tests"]
addopts = "-q"
