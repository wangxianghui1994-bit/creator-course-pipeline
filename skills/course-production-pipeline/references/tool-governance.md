# Tool governance

The bundled registry is a generic example. A project may provide a private
overlay registry with local renderers, but the overlay must remain outside a
public repository. The default adapter must be local and deterministic.

The Skill does not read credentials, visit platform pages, or mutate a draft.
Keep real course media, voice models, private paths, account state, and platform
screenshots out of examples and commits.
